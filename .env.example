# Generate secrets with: openssl rand -hex 64
JWT_SECRET=
ADMIN_SECRET=

# --- AI Provider Configuration ---
# Global default provider: "openai" or "anthropic"
# To swap to Anthropic later, change AI_PROVIDER=anthropic and set ANTHROPIC_API_KEY
AI_PROVIDER=openai
# OpenAI API key (required when AI_PROVIDER=openai)
API_KEY=
# Anthropic API key (required when AI_PROVIDER=anthropic)
ANTHROPIC_API_KEY=
# Default model (fallback when no per-use-case override matches)
MODEL_NAME=gpt-4o

# Per-use-case model overrides
# Provider is auto-detected from model name: "claude-*" → Anthropic, otherwise → OpenAI
#   lesson  = complex analysis (lesson generation, teacher briefings, learning paths)
#   assessment = complex analysis (placement, reassessment, diagnostics)
#   cheap   = frequent lightweight calls (warmups, summaries, recall, L1 analysis)
LESSON_MODEL=gpt-4o
ASSESSMENT_MODEL=gpt-4o
CHEAP_MODEL=gpt-4o-mini

# --- Database Configuration ---
# Option A: PostgreSQL (recommended for production)
# DATABASE_URL=postgresql://intake:your_password@localhost:5432/intake_eval
# Option B: SQLite (default, used when DATABASE_URL is empty)
DATABASE_PATH=intake_eval.db

# PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=intake
POSTGRES_PASSWORD=
POSTGRES_DB=intake_eval
